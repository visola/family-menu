import { inject, observer } from 'mobx-react';
import PropTypes from 'prop-types';
import React from 'react';

export default class DishPicker extends React.Component {
  render() {
    return <p>Dish Picker</p>;
  }
}